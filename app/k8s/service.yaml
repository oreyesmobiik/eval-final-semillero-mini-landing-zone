apiVersion: v1
kind: Service
metadata:
  name: miniapp
  namespace: miniapp
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    app: miniapp
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
